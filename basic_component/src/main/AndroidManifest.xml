<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hxh.component.basicore">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.access_wifi_state" />
    <uses-permission android:name="android.permission.access_network_state" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- <activity android:name=".ui.QRCode.activity.CaptureActivity"/> -->

        <activity android:name=".permissions.PermissionActivityActivity"
            android:theme="@style/Transparent_activity_theme"
        />
        <activity android:name=".CommonConponet.PdfViewerActivity" />

        <receiver android:name=".util.alarnclock.LoongggAlarmReceiver">
            <intent-filter>
                <action android:name="com.hxh.component.basicore" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".util.alarnclock.ClockAlarmActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <activity android:name=".CommonConponet.ImagePagerActivity" />

        <!--<receiver-->
            <!--android:name=".CommonConponet.NetworkConnectChangedReceiver"-->
            <!--&gt;-->
            <!--<intent-filter>-->
                <!--<action android:name="android.net.conn.CONNECTIVITY_CHANGE" />-->
                <!--<action android:name="android.net.wifi.WIFI_STATE_CHANGED" />-->
                <!--<action android:name="android.net.wifi.STATE_CHANGE" />-->
            <!--</intent-filter>-->

        <!--</receiver>-->
    </application>

</manifest>